#!/usr/bin/python3
"""This module creates a function for task 0"""


def lookup(obj):
    """
       A function that retrieves a list of the attributes and
       methods that are currently accessible to an object
    """
    return dir(obj)
